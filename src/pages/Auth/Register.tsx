import { logger } from "@/utils/";

export const Register = () => {
  const addToken = () => {
    logger;
  };

  <div>
    <input type="text" />
    <button onClick={addToken}>Send</button>
  </div>;
};
